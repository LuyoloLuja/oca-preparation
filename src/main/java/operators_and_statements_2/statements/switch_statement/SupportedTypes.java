package operators_and_statements_2.statements.switch_statement;

public class SupportedTypes {
//    single value is evaluated and flow is directed to the first matching case
//    if no value matches the case, an optional default value will be called
//    if default is not available, the whole switch statement will be skipped
//    default may appear anywhere in the switch

//    these are the only types that are supported in a switch
//    int -- Integer
//    byte -- Byte
//    short -- Short
//    char -- Character
//    String
//    enum values
}
